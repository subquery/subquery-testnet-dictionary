{
  "name": "moonbeam-dictionary",
  "version": "0.1.0",
  "description": "subquery diction for moonbase-alpha and moonriver",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "codegen": "./node_modules/.bin/subql codegen"
  },
  "homepage": "https://github.com/subquery/moonbeam-dictionary",
  "repository": "github:subquery/moonbeam-dictionary",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "Ian He",
  "license": "Apache-2.0",
  "devDependencies": {
    "@subql/contract-processors": "0.2.1-2",
    "@polkadot/api": "^6",
    "@subql/types": "latest",
    "typescript": "^4.1.3",
    "@subql/cli": "latest"
  }
}
