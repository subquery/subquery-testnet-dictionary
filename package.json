{
  "name": "subquery-testnet-dictionary",
  "version": "0.1.0",
  "description": "A dictionary for SubQuery testnet",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && tsc -b",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "codegen": "subql codegen"
  },
  "homepage": "https://github.com/subquery/subquery-testnet-dictionary",
  "repository": "github:subquery/subquery-testnet-dictionary",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "SubQuery network",
  "license": "Apache-2.0",
  "resolutions": {
    "@polkadot/api": "^7.12.1",
    "@polkadot/types": "^7.12.1",
    "@polkadot/util": "^8.5.1"
  },
  "devDependencies": {
    "@polkadot/api": "^7.12.1",
    "@subql/cli": "latest",
    "@subql/contract-processors": "latest",
    "@subql/types": "latest",
    "typescript": "^4.6.2"
  }
}
